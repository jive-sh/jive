name: Build & Deploy

on:
  push:
    branches:
      - main
    paths:
      - 'packages/**'
      - '.github/workflows/packages.yml'

jobs:
  compute-apps-to-rebuild:
    runs-on: ubuntu-latest
    outputs:
      changed-apps: ${{ steps.get-changed-apps.outputs.changed-apps }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
        with:
          # Needed to get analyze the latest commit (if any images changed)
          fetch-depth: 2

      - name: Get Changed Apps
        id: get-changed-apps
        run: |
          shopt -s expand_aliases
          source ./startup.sh
          chaine cicd packages apps

  build-and-deploy-apps:
    needs: compute-apps-to-rebuild
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.compute-apps-to-rebuild.outputs.changed-apps) }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Build & Publish & Deploy Website
        env:
          PUBLISH_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          shopt -s expand_aliases
          source ./startup.sh
          chaine dev @chaine-dance/apps.${{ matrix.app }} deps install
          chaine dev @chaine-dance/apps.${{ matrix.app }} build
          chaine dev @chaine-dance/apps.${{ matrix.app }} publish
          chaine dev @chaine-dance/apps.${{ matrix.app }} deploy
      # - name: Dev Env Deployment and Tests
      # - name: Preview Env Deployment and Tests
      # - name: Prod Env Deployment and Tests